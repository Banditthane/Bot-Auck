const serverService = require("../../../../domain/services/server.service");

module.exports = {
  name: "guildCreate",
  async execute(guild) {
    console.log(`🤖 Added to guild: ${guild.name}`);
    await serverService.registerGuild(guild.id);
  },
};
